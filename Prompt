1. 在家庭内网环境中有一台cannon打印机，已配置固定IP。
2. 在家庭内网的Nas服务器上安装了cups服务（部署在Docker中），并已完成打印机的添加，可以打印测试页。
3. 现在需要实现开发一给Web应用，用户可以通过该应用上传文件到Nas服务器，然后通过cups服务打印文件。
4. 该应用需要具备以下功能：
    - 用户可以通过该应用上传文件到Nas服务器，并选择打印机进行打印。
    - 用户可以通过该应用查看打印机状态，包括打印机是否在线、打印机是否正常工作等。 
    - 用户可以通过该应用查看打印机历史打印记录。
    - 用户可以通过该应用查看打印机当前打印任务，并对打印任务进行取消。
5. 该应用需要具备以下技术要求：
    - 该应用需要具备响应式设计，适应不同设备的屏幕大小。
    - 该应用需要具备良好的用户体验，操作简单明了。
    - 该应用需要具备良好的安全性，防止用户上传恶意文件。
    - 该应用可以通过docker容器化部署，方便后续的维护和更新。